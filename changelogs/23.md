### Features

- AFKReplace plugin will not replace users in the tutorial tower anymore
- Pushing is now disabled after the Warhead has detonated
- Added an semi-automated system for users to easily upload clips to open reports

### Fixes

- fixed some cache stuff in the frontend that resulted in falsy zvc values being shown
- age limit for setting ones birthday is now also enforced when using the discord command
